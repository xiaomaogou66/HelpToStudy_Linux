# 更新记录

## v0.3.3（2026-09-23）

补丁版：UTF-8 输出兜底（与 Windows 版 v1.1.2 同一修复）。

- `打卡.py`、`周文件夹.py`、`导出ics.py`、`sync_gcal.py` 启动时统一把 stdout/stderr
  重设为 UTF-8（`errors="replace"`）：输出被管道 / 重定向捕获时（CI、脚本互调、
  `> 日志.txt`）不再因为中文触发 `UnicodeEncodeError`

## v0.3.2（2026-09-23）

补丁版：时区兜底（与 Windows 版 v1.1.1 同一修复）。

- 新增 `get_tz()`：`ZoneInfo` 取不到时区库时退回固定偏移并提示 `pip install tzdata`，
  **时间照样正确**，装依赖不再是硬门槛；`打卡.py` / `导出ics.py` / `sync_gcal.py` 共用
  （Windows 上没装 `tzdata` 时原本会直接抛异常，ics 后端 / 打卡 / 同步全线失败）

## v0.3.1（2026-09-23）

补丁版：把 `today` 与 `自检` 收成**跨平台单份实现**（Windows 版 v1.1.0 与之共用），
并让新库开箱即用。

- `05-日程安排/_脚本/`：新增 `today.py`、`自检.py`（逻辑唯一实现），
  `today.sh` / `自检.sh` 改为调用它们的薄壳 —— 与 Windows 版 `today.bat` / `自检.bat`
  调同一份逻辑，两端不再各写一套
- `sync_gcal.py`：新增 `resolve_ics_paths()` —— 配置里的课表文件不存在时，
  自动退回同目录的 `课表示例.ics`，新装的库第一次跑 打卡 / 自检 / 同步就不会空转；
  `打卡.py` 与 `导出ics.py` 也走同一解析
- `05-日程安排/_脚本/requirements.txt`：补 `tzdata`（Windows 上 `ZoneInfo` 需要它，
  Linux 自带时区库，装了也无害）
- `_工具/split_textbook.py`：把「Linux 版」等平台字样改为中性描述，并与 Windows 版
  共用同一份策略说明（**全走 MinerU，不做本地提取**）
- `自检.py` 细节：缺依赖/缺令牌/缺虚拟环境一律标「⏭ 跳过」而不是失败；
  「统一入口」检查同时认 `.bat` / `.sh`

## v0.3.0（2026-09-23）

**新增第六层：日程安排（`05-日程安排/`）** —— 从「学得会」到「排得下」。
另有拆书脚本的一轮增强（章名匹配 + 备份出库），以及 `/拆书` 的令牌体检。

### 新层：日程安排（跨学期的时间预算与执行）

- 一周一个文件夹：`05-日程安排/06-日志/<周号>/` 里放 `<周号>.md`（周决策）+ `打卡.md`（打卡页）+ 7 篇日记
- `/周决策`（周日跑）：检索上周完成率、各科进度、检查点，生成下周周决策并预建周文件夹与日记
- **打卡项跟着课表走**：规则表在 `00-配置/底线规则.json`（`每节课` / `触发`（条件 → 打卡项）/
  `每天都有` / `例外.停课`），改规则不用改代码；课表里没有那门课，那天就不会出现那个项
- **两个地位同等的同步后端**（`_脚本/同步日程.sh`）：
  - `ics`：写本机 `_资源/日程安排.ics`，零云依赖，可被云盘 / Gist / 手机订阅
  - `gcal`：推 Google 专用日历（服务账号或 OAuth），多端原生同步
  - 两者读同一份配置、同一套 UID、同样的提醒口径；`--后端 ics|gcal` 可单选，`--dry-run` 只看不写
- **配置与引擎分家**：人只改 `05-日程安排/00-配置/`（`规划配置.json` 目标/里程碑/后端开关、
  `底线规则.json`、`同步配置.json`），引擎在 `_脚本/`；里程碑是**唯一真源**，
  主页倒计时 / 首页「今天」/ `today.sh` 三处都读它
- **`_脚本/自检.sh`（烟测）**：配置、打卡页、周文件夹、today.sh、两个后端、ics 结构、
  命令↔skill 一致性、MinerU 令牌，一条命令全过一遍（不联网、不改数据）
- 主页「📅 今天」区块：打卡 + 学习块两栏 Dataview 实时镜像，连续打卡天数自动算
- 教程：`00-使用指南/📖 使用说明.md`（首次配置 / 每天 / 每周 / 每次学习 / 每学期），
  多端同步与 Google 服务账号配置：`05-日程安排/00-配置/多端同步.md`
- 命名：这一层叫「日程安排」（`05-日程安排/`），主页 `日程安排.md`

### 拆书（`_工具/split_textbook.py`）

- **旧分块备份改到库外** `~/obsidian_backups/拆书/<书名>/`：不再在库里生成 `_备份/`
  （库内同名文件会在关系图谱里变成重复节点，也会让短链接解析有歧义）
- **章名匹配更稳**：正文标题只认出编号（`# 4 货币系统：…`、`# 13 重访开放经济: …`）时，
  先去掉「第N章 / N / N.」前缀再和目录名比，不再因为前缀差异漏掉章
- 书后部分关键词修正：补「术语表 / 译后记」，去掉会造成误切的「附录」

### 命令

- `/拆书` 增加**第 0 步令牌体检**：先查 MinerU Token 是否过期（`HTTP 401 + A0211` 判据，
  只查一个不存在的任务，不消耗额度、不改文件），报「可用 / 已过期 + 已签发多少天」；
  过期就停下来提示续期（重跑 `_工具/设置MinerU令牌.sh`），不会白跑一遍整本书
- `/学案` 支持**对照教材**（主题主页 frontmatter 有 `book_alt` 时同时读对照教材，做英文术语对照）；
  典型例题要求「教材原题照抄 + 解题思路」，自编题必须标注
- `/新主题-拆解与计划`：学习计划固定命名为 `【主题名】-学习计划.md`，并同步修正主页里的旧链接
- `/更新进度`、`/学案` 兼容新旧两种计划文件名

### 其他

- `AGENTS.md` / `CLAUDE.md`：写入规则改为「备份放库外 `~/obsidian_backups/`，禁止库内 `_备份/`」，
  并补上日程安排层的完整约定
- `.obsidian/`：新增 iCal（本地 .ics 订阅）、LaTeX Suite、Style Settings、Table Editor 四个插件
- 安装器：目录结构加入 `05-日程安排/` 与 `.pi/`，去掉已废弃的 `_备份/`，
  并明确不带入任何日志/课表/密钥；装完可跑 `05-日程安排/_脚本/自检.sh` 验证
- 仓库维护：`scripts/sync-from-vault.sh`（从本地库单向同步框架文件 + 通用化 + 上线前检查）

## v0.2.0（2026-08-30）

拆书（`_工具/split_textbook.py`、`_工具/拆书.sh`）：扫描版大书终于能一次跑通了。

### 上传：不再被超时掐断

- 上传分块改为「≤200 页 **且** ≤25 MB」双上限，单份超过体积自动二分切小
  （新增 `--mineru-chunk-mb`，0 = 不限体积）
- 扫描页太大时自动用 Ghostscript 降到 200 DPI 再分块（新增 `--mineru-dpi`，
  0 = 不压缩直传）：实测某 305 页扫描教材 191.5 MB → 63.2 MB / 132 秒，
  中西文、重音符号、音标识别质量无变化；降采样结果缓存在 `_工具/.mineru_cache/`，
  同一本书重跑不压第二次
- 某一份仍然上传超时 → 当场对半切小重传；Token/额度类错误立即判定，不再无谓重试
- `拆书.sh` 支持把额外参数透传给拆书脚本，例如
  `_工具/拆书.sh 某本书.pdf --mineru-chunk-mb 8`

### 章界：章标题被 OCR 打掉也能切准

- 新增「每章固定收尾小节」定位策略，并且**默认自动生效**：
  从正文里挖出每章都出现、只出现一次且间隔均匀的小节（如
  「作业 (Trabajos de casa)」），拿在每章最靠后的那个当章界锚点，
  章界 = 锚点之后的第一个章名行 / 章首小节
- 只有在通用标题识别明显失手（少于 4 章，或章数不到对方一半多一点）时才替换，
  正常教材的章表不受影响；`--chapter-end-pattern off` 可整体关掉，
  也可以自己给正则：`--chapter-end-pattern '习题\s*\(Ejercicios'`
  （另配 `--chapter-title-pattern` / `--chapter-start-pattern`）
- 章号从标题里抽取（`UNIDAD 8` / `UNIDAD8` / `第八课` / `第8课` 都认），
  章名按书里的写法自动生成（如 `UNIDAD 8 第八课`）
- 正文前的前言/目录不再被丢弃，单独存为「书前部分」分块；书后总词汇表等同理
- 切完自检：单章字数过少会告警，避免静默切错

### 其他

- 发布流水线：仓库里 7 个 `.sh` 的 git mode 改为 `100755`（此前 `./scripts/build-release.sh`
  在 Actions 里以 `exit 126 Permission denied` 失败，v0.1.0 起就一直没跑绿过），
  workflow 里也补了 `chmod +x`；发布步骤改成幂等（Release 已存在只覆盖资产）；
  `smoke-test` 冒烟作业改为只报告不卡发布（`continue-on-error`）
- `环境配置.sh` 增加 `ghostscript`（降采样依赖，缺失时自动退回原文件分块上传）
- 文档同步：`README.md`、`00-使用指南/📖 使用说明.md`、
  `01-提示词库/Step1-教材导入与拆书.md` 补上上述行为与两条常见问题

### 实测

《某外语教材 学生用书 1》305 页 / 191 MB、《学生用书 2》323 页 / 41 MB：
上传 0 次重试全部成功，各切出 16 课 + 书前部分 + 总词汇表，
每课恰好各含一个「作业 / 语法 GRAMÁTICA / 练习 EJERCICIOS」，
分块字数合计 = 解析全文字数（一字不漏不重）。

## v0.1.0（2026-08-12）

首个 Linux 版：Obsidian 库 + MinerU 拆书 + 五级水平拆解 + 二八学习计划 +
十问测试 + 一页速查表，附 `环境配置.sh` / `安装.sh` / `QuickInstall.sh`
一键安装与 GitHub Actions 发布流水线。
